{
  "name": "@mitchallen/grid",
  "version": "0.1.13",
  "description": "A 2D grid that uses zero-based indexing",
  "main": "modules/index.js",
  "scripts": {
    "//": "To use: npm run test-nodemon (assumes global location of mocha)",
    "start": "node index.js",
    "test": "mocha --recursive --timeout 20000",
    "test-nodemon": "nodemon /usr/local/bin/mocha --recursive --timeout 5000",
    "test-hexagon": "nodemon /usr/local/bin/mocha --recursive test/hexagon/ --timeout 5000",
    "test-square": "nodemon /usr/local/bin/mocha --recursive test/square/ --timeout 5000",
    "test-circle": "nodemon /usr/local/bin/mocha --recursive test/circle/ --timeout 5000",
    "test-triangle": "nodemon /usr/local/bin/mocha --recursive test/triangle/ --timeout 5000",
    "test-create": "nodemon /usr/local/bin/mocha --recursive test/create/ --timeout 5000",
    "test-debug": "nodemon /usr/local/bin/mocha -g '@DEBUG' --recursive --timeout 20000",
    "test-jenkins": "mocha --recursive --timeout 20000 -R xunit test/ > test-reports.xml"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/mitchallen/grid.git"
  },
  "author": "Mitch Allen <npm@mitchallen.com> (http://mitchallen.com)",
  "license": "ISC",
  "devDependencies": {
    "babel-preset-es2015": "^6.18.0",
    "babelify": "^7.3.0",
    "grunt": "^1.0.1",
    "grunt-browserify": "^5.0.0",
    "grunt-bump": "^0.8.0",
    "grunt-contrib-jshint": "^1.0.0",
    "grunt-contrib-uglify": "^2.0.0",
    "grunt-contrib-watch": "^1.0.0",
    "grunt-shell": "^1.3.0",
    "should": "^10.0.0",
    "supertest": "^2.0.0"
  }
}
